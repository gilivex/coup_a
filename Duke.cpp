#include <iostream>
#include <string>
#include "Duke.hpp"
#include "Player.hpp"

using namespace std;
namespace coup
{
    Duke Duke : (Game game, string name) : Player(game, name)
    {
    }
    void Duke::tax()
    {
    }
    void Duck::block(Player)
    {
    }
};
